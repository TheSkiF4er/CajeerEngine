# Журнал изменений Cajeer Engine

Все заметные изменения в **Cajeer Engine** документируются в этом файле.

Формат вдохновлён [Keep a Changelog](https://keepachangelog.com/ru-ru/1.1.0/)
и мы стремимся следовать принципам [Semantic Versioning](https://semver.org/lang/ru/).

- Формат версии: `MAJOR.MINOR.PATCH`
- Даты указываются в формате `ДД-ММ-ГГГГ`
- Раздел **[Unreleased]** описывает состояние текущей ветки разработки

---

## 🔄 [Unreleased]

> Текущее состояние ветки `main` (открытая третья версия Cajeer Engine).

### Добавлено

- Переработанное ядро 3-го поколения:
  - собственный роутер и front-controller
  - модульная архитектура ядра и системных модулей
  - подсистема безопасности (авторизация, роли, базовая защита от CSRF)
  - консольные утилиты (`/cli`) для установки и обслуживания
- Новый шаблонизатор `.tpl`:
  - теги в духе DLE (`{title}`, `{content}`, `{user.name}`, `{config.site_name}`)
  - условные блоки `[if]`, `[else]`, `[group]`, `[not-group]`, `[available]`
  - модульные теги `{module:news}`, `{module:comments}` и т.п.
  - компиляция шаблонов в PHP и кеширование
- Модульная система:
  - разделение на **системные** модули (`/cms/engine/modules`)
  - поддержка внешних модулей разработчиков (`/modules`)
  - базовые модули: новости, пользователи, комментарии, статические страницы (структура и точки расширения)
- Структурированная файловая архитектура:
  - `/cms/engine` — ядро CMS
  - `/cms/templates` — шаблоны сайта и админ-панели
  - `/dbs` — «чистая» база для установки
  - `/modules` — внешние модули
- Система локализации:
  - языковые файлы для ядра и модулей
  - поддержка нескольких языков (EN, ES, RU, FR, DE, IT)
- Система обновлений:
  - форматы пакетов `.cajeerpatch` (обновления) и `.cajeerpkg` (языки и компоненты)
  - поддержка обновлений через админ-панель и CLI
- Документация:
  - обновлённый `README.md` с историей проекта и архитектурой
  - базовые разделы в Wiki (архитектура, разработка модулей, шаблоны)

### Изменено

- Архитектура ядра полностью переосмыслена и адаптирована под открытый исходный код.
- Структура проекта унифицирована с другими продуктами линейки Cajeer, но без привязки к устаревшим компонентам прошлых поколений.
- Улучшена читаемость и организация кода для разработки модулей и тем.

### Удалено

- Всё наследие предыдущих закрытых версий (старое ядро, устаревшие модули и шаблоны) не перенесено в open-source-ветку: проект начинается как чистая переработка.

---

## 🧭 Планируемые релизы (Roadmap)

> Ниже — ориентировочный план версий. По мере выхода реальных релизов эти записи
> будут перенесены в отдельные секции с фактическими датами и изменениями.

### [0.1.0] — Базовое ядро

- ядро CMS, роутер, базовые модули (страницы/новости)
- `.tpl`-шаблонизатор и базовые теги
- структура проекта и установщик
- минимальная админ-панель

### [0.2.0] — Пользователи и админка

- полноценный модуль пользователей и авторизации
- система ролей и прав доступа
- расширенная админ-панель
- улучшенная безопасность (сессии, CSRF, базовая защита от brute force)

### [0.3.0] — Обновления и локализация

- готовая система обновлений и откатов через CLI и админ-панель
- языковые пакеты для ядра и модулей
- улучшенный DX для перевода (единая структура языковых файлов)

### [1.0.0] — Стабильный релиз

- зафиксированные API для модулей и тем
- полная документация по:
  - архитектуре ядра
  - созданию модулей
  - созданию шаблонов
  - системе обновлений
- набор референс-модулей и базовая тема как пример для разработчиков
