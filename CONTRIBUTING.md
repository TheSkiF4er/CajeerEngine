# Вклад в развитие Cajeer Engine

Спасибо, что хотите помочь развитию **Cajeer Engine** 🙌  
Этот документ рассказывает, как лучше всего вносить вклад в проект, чтобы ваше время и усилия принесли максимум пользы.

> Коротко: делайте маленькие, понятные изменения, оформляйте их аккуратно и соблюдайте уважительный тон общения.

---

## 1. Прежде чем начать

1. Ознакомьтесь с:
   - `README.md`
   - `CODE_OF_CONDUCT.md`
   - документацией / Wiki (особенно разделами про архитектуру, модули и шаблоны)

2. Определите, что именно вы хотите сделать:
   - исправить баг 🐞
   - добавить/улучшить фичу ✨
   - дописать документацию 📚
   - сделать новый модуль или шаблон 🎨
   - помочь с переводом 🌍
   - заняться рефакторингом или оптимизацией ⚙️

3. Если изменение **крупное** (новый модуль, серьёзный рефакторинг ядра и т.п.) —:
   - создайте issue с предложением,
   - кратко опишите идею и мотивацию,
   - дождитесь первичного обсуждения, чтобы не тратить силы зря.

---

## 2. Как можно помочь

Вот несколько способов, которыми вы можете реально улучшить Cajeer Engine:

- **Сообщать об ошибках**
  - Неправильная работа функций, модулей, шаблонов
  - Ошибки установки или обновления
- **Разрабатывать новые фичи**
  - Модули, виджеты, интеграции
  - Улучшения панели администратора
- **Создавать и улучшать шаблоны**
  - Новые темы оформления
  - Удобные блоки/компоненты интерфейса
- **Помогать с локализацией**
  - Добавление новых языков
  - Актуализация существующих переводов
- **Улучшать документацию**
  - Пошаговые гайды
  - Примеры использования API, модулей, тегов
- **Работать над производительностью и безопасностью**
  - Оптимизация запросов
  - Повышение безопасности ядра и модулей

Любой вклад важен — даже исправление опечаток.

---

## 3. Локальная среда разработки

Рекомендуемое окружение:

- PHP **8.1+**
- MySQL / MariaDB
- Web-сервер (Nginx / Apache) или встроенный PHP-сервер
- Git
- Composer (если используется в репозитории)

### Быстрый старт

```bash
git clone https://github.com/TheSkiF4er/CajeerEngine.git
cd CajeerEngine

git checkout -b feature/my-awesome-feature
````

Далее:

1. Настройте БД (создайте базу, пользователя).
2. Настройте виртуальный хост или PHP built-in server.
3. Выполните шаги установки Cajeer Engine согласно документации.
4. Проверьте, что движок устанавливается и открывается в браузере.

---

## 4. Стиль кода и архитектура

### 4.1. Общие принципы

* Пишите **читаемый и понятный код** — для будущего себя и других.
* Следуйте существующей структуре каталогов:

  * `engine/` — ядро
  * `engine/modules/` — системные модули
  * `modules/` — внешние/пользовательские модули
  * `templates/` — шаблоны `.tpl`
  * `storage/` — кэш, логи, скомпилированные шаблоны
* Избегайте копипасты — выносите общие части в отдельные методы/классы.

### 4.2. PHP

* Ориентируемся на современные рекомендации (PSR-12 и здравый смысл):

  * аккуратные отступы и форматирование,
  * понятные имена переменных, методов и классов,
  * использование type hints и return types там, где это возможно.
* Не смешивайте бизнес-логику и вывод:

  * Логика — в ядре и модулях,
  * Шаблоны — только отображение и шаблонные теги.

### 4.3. Шаблоны `.tpl`

* Используйте шаблонные теги Cajeer Engine:

  * `{title}`, `{content}`, `{user.name}`, `{config.site_name}` и т.п.
  * условные блоки `[if]`, `[else]`, `[group]`, `[not-group]` и др.
* Не добавляйте тяжёлую PHP-логику в шаблоны.
* Старайтесь, чтобы шаблоны были универсальными и легко переиспользуемыми.

---

## 5. Модули, расширения и темы

### 5.1. Модули

При добавлении или изменении модуля:

* Следуйте структуре существующих модулей в `engine/modules/`.
* Разделяйте:

  * фронтовую часть (контроллеры, модели, шаблоны),
  * админскую часть (панель управления),
  * языковые файлы,
  * установочные/миграционные скрипты (если есть).
* Старайтесь, чтобы модуль:

  * был максимально **самодостаточным**,
  * не “ломал” ядро,
  * не требовал произвольных прав, если это не нужно.

### 5.2. Темы и шаблоны

* Располагайте темы в `templates/имя_темы/`.
* Следите, чтобы тема:

  * корректно работала с основными сущностями движка (страницы, меню, блочные элементы),
  * не содержала жёстко прописанных текстов, которые должны идти через систему локализации.
* Минимизируйте дублирование кода между шаблонами.

---

## 6. Локализация (переводы)

Cajeer Engine изначально поддерживает несколько языков.

Если вы:

* добавляете новый язык,
* улучшаете существующий перевод,
* обновляете строки для нового функционала,

то:

1. Сохраните структуру и ключи существующих языковых файлов.
2. Убедитесь, что все нужные ключи есть во всех поддерживаемых языках.
3. Проверяйте, как строки выглядят в интерфейсе (и в публичной части, и в админке).
4. Избегайте слишком “жаргонных” выражений — текст должен быть понятным и нейтральным.

---

## 7. Issues: как правильно создать задачу

Перед созданием issue:

1. Проверьте, нет ли уже подобной задачи.
2. Убедитесь, что вы используете **актуальную версию** Cajeer Engine.

### 7.1. Сообщение об ошибке

Постарайтесь указать:

* версию Cajeer Engine / ветку;
* версию PHP, СУБД и тип веб-сервера;
* шаги для воспроизведения **по пунктам**;
* ожидаемый результат;
* фактический результат;
* логи/скриншоты, если они помогают понять проблему.

Хорошо оформленный баг-репорт экономит всем много времени.

### 7.2. Предложение фичи

Опишите:

* **Проблему**: что сейчас неудобно или чего не хватает.
* **Предлагаемое решение**: как вы видите реализацию.
* **Область применения**: кому и чем это поможет.
* При возможности — примеры интерфейса или API.

---

## 8. Pull Requests: как оформить изменения

### 8.1. Ветки

Рекомендуемые префиксы:

* `fix/...` — исправление ошибок
* `feature/...` — новая функциональность
* `refactor/...` — рефакторинг без изменения поведения
* `docs/...` — документация
* `chore/...` — мелочи, не влияющие на логику

Примеры:

* `fix/install-php82-warning`
* `feature/news-module`
* `docs/update-installation-guide`

### 8.2. Коммиты

Старайтесь делать:

* небольшие, логически связанные коммиты,
* осмысленные сообщения, например:

  * `Fix: обработка пустого заголовка страницы`
  * `Add: базовая страница настроек модуля news`
  * `Docs: описан процесс обновления через CLI`

### 8.3. Чек-лист перед отправкой PR

Перед тем, как создать Pull Request:

* [ ] Код **собирается и запускается** без фатальных ошибок.
* [ ] Базовый функционал, который вы трогали, **проверен вручную**.
* [ ] При необходимости обновлена документация (README, Wiki и т.п.).
* [ ] Нет лишних отладочных `var_dump`, `die`, `echo` и т.п.
* [ ] Нет явно временного кода и закомментированных “кусков на всякий случай”.

### 8.4. Описание PR

В описании Pull Request укажите:

* кратко: **что** сделано (1–3 предложения);
* ссылку на issue, если PR его закрывает (например, `Closes #42`);
* короткий список изменений:

  * добавлено / изменено / удалено;
* есть ли breaking changes — изменения, ломающие обратную совместимость.

---

## 9. Безопасность

Если вы нашли потенциальную **уязвимость безопасности**:

* Не создавайте публичный issue с подробностями.
* Напишите мейнтейнерам напрямую через указанные в репозитории контакты.
* Укажите:

  * шаги воспроизведения,
  * возможное влияние (XSS, SQLi, RCE, LFI и т.д.),
  * рекомендации по исправлению (если есть идеи).

Мы очень ценим ответственное раскрытие уязвимостей.

---

## 10. Лицензия и права

Внося вклад в репозиторий Cajeer Engine (через PR, патчи и т.п.), вы подтверждаете, что:

* имеете право распространять этот код;
* согласны на использование и распространение вашего вклада в рамках лицензии проекта (см. файл `LICENSE`).

Если что-то непонятно по лицензированию, задайте вопрос в issue.

---

## 11. Благодарности

Cajeer Engine — это результат многолетнего опыта и работы, но развивать его дальше можно только вместе с сообществом.

Каждый:

* найденный и описанный баг,
* новый модуль или шаблон,
* улучшенный перевод,
* доработанный кусок документации,
* аккуратный PR

делает движок лучше для всех — разработчиков, администраторов и пользователей.

Спасибо, что вкладываетесь в Cajeer Engine 💙
Добро пожаловать в сообщество и приятного вам контрибьютинга!
