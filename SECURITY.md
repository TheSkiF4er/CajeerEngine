# 🛡 SECURITY.md — Политика безопасности Cajeer Engine

Этот документ описывает подход к безопасности в **Cajeer Engine** и объясняет, как сообщать об уязвимостях, получать исправления и безопасно использовать движок в продакшене.

---

## 🔍 Обзор

**Cajeer Engine** — это open‑source CMS на PHP и MySQL, разработанная командой **Cajeer 404** и автором **SkiF4er**, распространяемая под лицензией **Apache 2.0**.

Внутри ядра предусмотрены отдельные подсистемы безопасности:

- `engine/security/auth.php` — аутентификация и сессии  
- `engine/security/csrf.php` — защита от CSRF  
- `engine/security/sanitizer.php` — фильтрация пользовательского ввода  
- `engine/security/hash.php` — безопасное хеширование паролей  
- `engine/security/firewall.php` — простая защита от флуда/подбора/подозрительных запросов  

Тем не менее, никакая система не идеальна, и мы всегда открыты ответственному сообщению о проблемах безопасности.

---

## 📬 Как сообщить о проблеме безопасности

Пожалуйста, **НЕ создавайте публичный issue** с описанием уязвимости, пока она не будет исправлена.  
Вместо этого:

1. Напишите на email по безопасности проекта:  
   **`security@cajeer.ru`**  

2. В письме укажите:

   - версию Cajeer Engine (`/system/version.txt`);  
   - тип установки (чистая / с кастомными модулями / с изменённым ядром);  
   - краткое описание проблемы;  
   - шаги для воспроизведения (желательно максимально детализированные);  
   - возможное влияние (RCE, SQLi, XSS, обход авторизации и т.п.);  
   - при наличии — PoC (Proof of Concept) и логи/скриншоты.

3. Если уязвимость критическая, пометьте тему письма, например:  
   `"[SECURITY] Critical RCE in Cajeer Engine"`.

Мы стремимся:

- подтвердить получение сообщения в разумные сроки;  
- воспроизвести проблему;  
- подготовить исправление и/или временный обходной путь;  
- выпустить обновление в виде `.cajeerpatch` и описать проблему в changelog’е.

---

## ✅ Что считается уязвимостью

Примеры проблем, которые **точно стоит** нам сообщить:

- **Удалённое выполнение кода (RCE)**  
- **SQL-инъекции** (в ядре или стандартных модулях)  
- **XSS** (stored / reflected) в публичной части или админке  
- **Обход аутентификации / авторизации**  
- **Повышение привилегий** (например, пользователь может получить права администратора)  
- **Обход CSRF-защиты** в критичных действиях (изменение пароля, настроек сайта, прав и т.п.)  
- Утечка конфиденциальных данных через API или системные эндпоинты  
- Логика, позволяющая обходить ограничения доступа к модулям, разделам, шаблонам

---

## 🚫 Что обычно **не** считается уязвимостью ядра

- Небезопасная конфигурация сервера (открытые порты, старый OpenSSL, отсутствие HTTPS и т.п.)  
- Использование устаревших версий PHP/MySQL, не поддерживаемых самим вендором  
- Слабые пароли админа (например, `admin/admin` или `123456`)  
- Уязвимости в **кастомных внешних модулях** (`/modules`), написанных сторонними разработчиками  
- Некорректные права на файлы и папки, выставленные вручную (например, `777` на всё)  
- Демо-конфигурации и тестовые учетные записи, оставленные в продакшене  
- Уязвимости, возникающие только при намеренном отключении встроенных механизмов безопасности (CSRF, Firewall, проверки сессий и т.п.)

Тем не менее, если вы сомневаетесь — лучше напишите, разберём вместе.

---

## 🤝 Ответственное раскрытие (Responsible Disclosure)

Мы придерживаемся модели ответственного раскрытия уязвимостей:

1. Вы конфиденциально делитесь детальной информацией об уязвимости с командой Cajeer Engine.
2. Мы анализируем, подтверждаем и подготавливаем исправление.
3. Вы даёте нам разумное время на выпуск патча (например, **30–90 дней** в зависимости от критичности).
4. После выхода обновления и рекомендаций по защите:
   - Вы можете публично описать уязвимость (по согласованию).
   - Мы можем вас указать в списке благодарностей (если вы не возражаете).

Мы **уважаем труд исследователей**, поэтому:

- не преследуем тех, кто добросовестно и ответственно сообщает о проблемах;  
- готовы указать вас в качестве автора найденной уязвимости.

---

## 🧩 Безопасность модулей и шаблонов

Cajeer Engine изначально спроектирован как модульная CMS с `.tpl`‑шаблонизатором. Это удобно, но добавляет рисков:

- любые PHP‑вставки, исполнение произвольного кода, прямые SQL‑запросы в модулях — источник потенциальных RCE/SQLi;  
- шаблоны `.tpl` при некорректной фильтрации пользовательских данных могут становиться источником XSS.

Рекомендации:

- отдавайте предпочтение **стандартным модулям** (`/engine/modules/...`);  
- при установке сторонних модулей (`/modules`) изучайте код и источник;  
- избегайте вставки "сырого" PHP‑кода в шаблоны;  
- всегда используйте API ядра (sanitizer, auth, csrf, firewall) вместо самописных решений внутри модулей.

---

## 🔐 Рекомендации по безопасной конфигурации

Минимальный чек‑лист для продакшен‑установки:

1. **PHP и MySQL**
   - Используйте **актуальные поддерживаемые версии** PHP и MySQL/MariaDB.  
   - Включите `display_errors = Off` в продакшене.

2. **Файловые права**
   - Права на запись только там, где это необходимо:  
     - `/storage`  
     - `/uploads`  
     - `/packages` (если используете систему обновлений)  
   - Исходники ядра `/engine` и конфиги `/system` должны быть **только для чтения** пользователю веб-сервера.

3. **Веб‑сервер**
   - Настройте веб‑корень на каталог `/public`.  
   - Запретите прямой доступ к:
     - `/engine`
     - `/system`
     - `/storage`
     - `/cli`
     - `/ci`
   - Включите **HTTPS** во всех средах, где есть авторизация.

4. **Админ-панель**
   - Измените URL по умолчанию (например, `/admin.php` → `/my-admin.php` через конфиг или rewrite).  
   - Используйте сложный пароль и, по возможности, двухфакторную аутентификацию (если будет реализована).  
   - Ограничьте доступ к админке по IP/географии (на уровне веб-сервера или WAF).

5. **Бэкапы и обновления**
   - Регулярно создавайте бэкапы `/system`, `/storage`, `/uploads` и базы данных.  
   - Перед установкой `.cajeerpatch` — делайте резервную копию.  
   - Не откладывайте установку критических патчей.

---

## 🧪 Тестирование безопасности

Если вы проводите аудит/тестирование:

- по возможности выполняйте тесты **на отдельном стенде**, а не в продакшене;
- не проводите DDoS-тесты и нагрузочные атаки на боевые системы без согласования;
- в отчётах разделяйте:
  - проблемы конфигурации сервера;
  - уязвимости ядра Cajeer Engine;
  - уязвимости кастомных модулей/шаблонов.

---

## 📦 Обновления и версии

Информация о версии ядра:

- файл: `/system/version.txt`  
- канал обновлений: `/system/update_channel.txt` (`stable`, `beta`, `alpha`)

Обновления распространяются в виде:

- `.cajeerpatch` — обновления ядра/модулей/шаблонов  
- `.cajeerpkg` — языковые пакеты и дополнительные компоненты

Рекомендуется всегда использовать **stable‑ветку**, если вы не занимаетесь разработкой ядра.

---

## 📣 Обратная связь и улучшения

Если у вас есть идеи по:

- усилению механизмов безопасности;
- улучшению CSRF/XSS‑защиты;
- добавлению новых политик или расширению этого SECURITY.md;

вы можете:

- предложить изменения через pull‑request к этому файлу;  
- обсудить подход в issue‑трэкер (без раскрытия конкретных уязвимостей);  
- написать в контакт по безопасности.

---

## 🧾 Резюме

- Cajeer Engine серьёзно относится к безопасности и открыт к сотрудничеству с исследователями.
- Пожалуйста, используйте **ответственное раскрытие** и не публикуйте детали уязвимостей до выхода исправлений.
- Безопасность — это совместная работа ядра, окружения, конфигурации и людей, которые всё это настраивают.

Спасибо, что помогаете сделать **Cajeer Engine** безопаснее. 🙌
